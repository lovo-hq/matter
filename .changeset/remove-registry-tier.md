---
'@camp-dev/shaders-cli': patch
---

`shaders-cli list` prints each component's slug and description only. The "tier 1" suffix is gone, along with the always-1 `tier` field it printed from the registry manifest.
