{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "type": "object",
  "properties": {
    "$schema": {
      "type": "string"
    },
    "version": {
      "type": "string",
      "minLength": 1,
      "description": "Registry format version, independent of the package versions."
    },
    "components": {
      "type": "object",
      "propertyNames": {
        "type": "string"
      },
      "additionalProperties": {
        "$ref": "#/definitions/entry"
      },
      "description": "Component slug to entry. The slug is what a user passes to `shaders-cli add`."
    }
  },
  "required": [
    "version",
    "components"
  ],
  "additionalProperties": false,
  "$id": "https://github.com/campdotdev/shaders/blob/main/registry/registry.schema.json",
  "title": "Shaders registry index",
  "description": "The index @camp-dev/shaders-cli reads to copy Tier 1 components into a user's project. Generated from registrySchema in apps/docs/src/content/schema.ts by the registry-schema test; edit that file and rerun the test with -u rather than editing this one.",
  "definitions": {
    "entry": {
      "type": "object",
      "properties": {
        "file": {
          "description": "Entry point, relative to the registry root: the wrapper the user imports.",
          "allOf": [
            {
              "$ref": "#/definitions/sourcePath"
            }
          ]
        },
        "files": {
          "type": "array",
          "items": {
            "$ref": "#/definitions/sourcePath"
          },
          "description": "Every other source the component needs, relative to the registry root: its shader, plus any shared helper under utils/. The CLI leaves relative import specifiers alone, so anything a listed file imports must itself be listed or the install will not compile.",
          "uniqueItems": true
        },
        "description": {
          "type": "string",
          "minLength": 1,
          "description": "One line, shown by `shaders-cli list`."
        },
        "dependencies": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "description": "npm packages the component needs. Printed as an install hint after `shaders-cli add`.",
          "uniqueItems": true
        },
        "uses_primitives": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "description": "Tier 2 primitives the component builds on. Documentation only; the CLI does not read it.",
          "uniqueItems": true
        },
        "category": {
          "type": "string",
          "enum": [
            "gradients",
            "noise",
            "patterns",
            "scenes",
            "lens-film",
            "retro-glitch"
          ],
          "description": "The leaf group the docs sidebar files the component under. The label, tier, and order of each group live in apps/docs/src/content/taxonomy.ts; the CLI does not read it."
        }
      },
      "required": [
        "file",
        "description",
        "dependencies",
        "category"
      ],
      "additionalProperties": false
    },
    "sourcePath": {
      "type": "string",
      "pattern": "^(?!\\/)(?!.*(^|\\/)\\.\\.(\\/|$)).*\\.tsx?$",
      "description": "A .ts or .tsx path relative to the registry root. Must stay inside it: the CLI writes these into a user's project, and refuses any path that resolves outside the configured components directory."
    }
  }
}
